name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build
        run: apt update && apt install -y libopengl-dev
        run: cmake ./CMakeLists.txt && make
